/*#ifndef _VALIDATOR_H
#define _VALIDATOR_H
#include <string>
using namespace std;
class Validator
{
public:
    Validator();
    ~Validator();
    bool recordExists(string);
};

#endif*/
